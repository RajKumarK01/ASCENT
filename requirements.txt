# Core agent framework + Foundry
agent-framework
azure-ai-projects>=2.1.0
azure-identity

# Foundry IQ / knowledge base retrieval
azure-search-documents

# Hosted Agent protocol (Responses) — enable when deploying
# azure-ai-agentserver-responses

# Telemetry
azure-monitor-opentelemetry

# BFF (FastAPI server)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
bcrypt>=4.1.0
PyJWT>=2.8.0
httpx>=0.27.0

# Utilities
python-dotenv
